/**
 *  1. go to firebase.com and then go to console
 *  2. create a firebase project
 *  3. go to web settings for firebase app and register the app
 *  4. npm install firebase
 *  5. get firebase config and [you should not] sdd the config in your project [do not save or share your config]
 *  6. enable sign in method that you want to use: Build > Authentication > Get Started > Click > on Google > Enable > set the email address > save
 *  7. go to firebase docs
 *  8. create auth (firebase initialize auth instance)
 *  9. go to the login button. add even handler and create a provider (google provider)
 *  10. call the signInWithProvider (auth, googleProvider)
 *  11. after successfully getting data, you may set it to a state
 */